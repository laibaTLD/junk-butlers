'use client';

// This file previously contained a complex ServiceDetails component that relied
// on many extracted components from a different folder. Since dynamic service
// detail pages are no longer used, we keep only a tiny placeholder component so
// the file compiles without any external dependencies.

export default function ServiceDetailsPlaceholder() {
  return null;
}

